<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estadísticas | SIGAN</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        .stats-container { max-width: 1000px; margin: 20px auto; padding: 20px; font-family: sans-serif; }
        .grid-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 20px; }
        .card { background: white; padding: 20px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); text-align: center; }
        .impact-card { grid-column: span 2; background: #1b4332; color: white; padding: 25px; }
        .impact-number { font-size: 2.5em; font-weight: bold; color: #b7e4c7; }
        .chart-container { position: relative; height: 300px; width: 100%; }
        .btn-back { display: inline-block; margin-bottom: 20px; color: #1b4332; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body style="background-color: #f4f7f6;">

    <div class="stats-container">
        <a href="/dashboard" class="btn-back">← Volver al Dashboard</a>
        
        <h1>Análisis de Inventario: <%= info.nombre_finca %></h1>
        <p>Reporte estadístico detallado de su unidad de producción.</p>

        <div class="grid-stats">
            <div class="card impact-card">
                <h3>Tu Impacto Regional</h3>
                <p>En el estado <strong><%= info.estado %></strong>, se han registrado un total de <%= info.totalEstado %> animales.</p>
                <div class="impact-number">
                    <%= ((info.misAnimales / info.totalEstado) * 100).toFixed(2) %>%
                </div>
                <p>Es la cuota de participación de tu finca en el inventario estadal de SIGAN.</p>
            </div>

            <div class="card">
                <h3>Distribución por Sexo</h3>
                <div class="chart-container">
                    <canvas id="chartSexo"></canvas>
                </div>
            </div>

            <div class="card">
                <h3>Distribución por Razas</h3>
                <div class="chart-container">
                    <canvas id="chartRaza"></canvas>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Datos para Sexo
        const dataSexo = {
            labels: [<% statsSexo.forEach(s => { %> '<%= s.sexo %>', <% }) %>],
            datasets: [{
                data: [<% statsSexo.forEach(s => { %> <%= s.cantidad %>, <% }) %>],
                backgroundColor: ['#2d6a4f', '#74c69d', '#1b4332', '#95d5b2'],
                borderWidth: 1
            }]
        };

        // Datos para Razas
        const dataRaza = {
            labels: [<% statsRaza.forEach(r => { %> '<%= r.raza %>', <% }) %>],
            datasets: [{
                label: 'Cantidad de Animales',
                data: [<% statsRaza.forEach(r => { %> <%= r.cantidad %>, <% }) %>],
                backgroundColor: '#40916c'
            }]
        };

        // Renderizar Gráfica de Sexo
        new Chart(document.getElementById('chartSexo'), {
            type: 'doughnut',
            data: dataSexo,
            options: { maintainAspectRatio: false }
        });

        // Renderizar Gráfica de Razas
        new Chart(document.getElementById('chartRaza'), {
            type: 'bar',
            data: dataRaza,
            options: {
                maintainAspectRatio: false,
                scales: { y: { beginAtZero: true, ticks: { stepSize: 1 } } }
            }
        });
    </script>
</body>
</html>