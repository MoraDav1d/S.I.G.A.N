<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIGAN | Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="dashboard-container">
        <header class="dash-header">
            <h1>Bienvenido, <%= usuario %> 👋</h1>
            <p>Panel de Gestión de la Unidad de Producción</p>
        </header>

        <div class="dash-grid">
            <div class="dash-card main-info">
                <% if (finca) { %>
                    <h3><i class="fa-solid fa-tractor"></i> <%= finca.nombre_finca %></h3>
                    
                    <div class="finca-details">
                        <p><strong><i class="fa-solid fa-location-dot"></i> Ubicación:</strong> <%= finca.municipio %>, <%= finca.estado %></p>
                        <p><strong><i class="fa-solid fa-id-badge"></i> Propietario:</strong> <%= finca.nombre_prop %> <%= finca.apellido_prop %></p>
                    </div>

                    <div style="background: var(--verde-claro); color: white; padding: 15px; border-radius: 10px; margin-top: 15px; display: flex; align-items: center; gap: 15px;">
                        <i class="fa-solid fa-cow" style="font-size: 2rem;"></i>
                        <div>
                            <h4 style="margin: 0;">Inventario Actual</h4>
                            <p style="margin: 0; font-size: 1.2rem; font-weight: bold;"><%= totalAnimales %> Ejemplares</p>
                        </div>
                    </div>

                    <div class="hierro-preview">
                        <p><i class="fa-solid fa-stamp"></i> Hierro Registrado</p>
                        <img src="/uploads/<%= finca.hierro_img %>" alt="Hierro de la finca">
                    </div>
                <% } else { %>
                    <div class="no-data">
                        <h3>Bienvenido al Sistema</h3>
                        <p>Aún no has completado el registro legal de tu unidad de producción.</p>
                        <a href="/registro-finca" class="btn-entrar-dash">Registrar Finca Ahora</a>
                    </div>
                <% } %>
            </div>

            <div class="dash-card shortcuts">
                <h3><i class="fa-solid fa-bolt"></i> Acciones Rápidas</h3>
                <div class="btn-group">
                    <a href="/registrar-ganado" class="btn-dash" style="text-decoration: none; display: block; width: 100%;">
                        <i class="fa-solid fa-plus"></i> Registrar Ganado
                    </a>

                    <a href="/estadisticas" class="btn-dash" style="text-decoration: none; display: flex; align-items: center; justify-content: center; background-color: #40916c;">
                        <i class="fa-solid fa-chart-line"></i> Estadísticas Finca
                    </a>
                   
                    <a href="/reporte-sigan" target="_blank" class="btn-dash" style="text-decoration: none; display: flex; align-items: center; justify-content: center;">
                        <i class="fa-solid fa-file-pdf"></i> Reporte SIGAN
                    </a>
                </div>
            </div>
        </div>

        <div class="dash-card" style="margin-top: 20px; width: 100%; overflow-x: auto;">
            <h3><i class="fa-solid fa-list"></i> Inventario de Ganado</h3>
            
            <% if (animales.length > 0) { %>
                <table style="width: 100%; border-collapse: collapse; margin-top: 15px;">
                    <thead>
                        <tr style="background-color: #2d6a4f; color: white; text-align: left;">
                            <th style="padding: 10px;">ID / Arete (Origen)</th>
                            <th style="padding: 10px;">Nombre</th>
                            <th style="padding: 10px;">Raza</th>
                            <th style="padding: 10px;">Sexo</th>
                            <th style="padding: 10px;">Uso / Propósito</th>
                            <th style="padding: 10px;">Peso (Kg)</th>
                            <th style="padding: 10px;">Fecha Nac.</th>
                            <th style="padding: 10px; text-align: center;">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% animales.forEach(animal => { %>
                            <tr style="border-bottom: 1px solid #ddd;">
                                <td style="padding: 10px;">
                                    <span style="color: #666; font-size: 0.85rem; font-weight: normal;">
                                        <%= (animal.codigo_estado || 0).toString().padStart(2, '0') %> -
                                    </span>
                                    <strong><%= animal.codigo_arete %></strong>
                                </td>
                                <td style="padding: 10px;"><%= animal.nombre_animal || 'N/A' %></td>
                                <td style="padding: 10px;"><%= animal.raza %></td>
                                <td style="padding: 10px;"><%= animal.sexo %></td>
                                <td style="padding: 10px;">
                                    <span style="background: #e9f5ee; color: #2d6a4f; padding: 4px 8px; border-radius: 5px; font-size: 0.8rem;">     
                                        <%= animal.proposito || 'No asignado' %>
                                    </span>
                                </td>
                                <td style="padding: 10px;"><%= animal.peso_inicial %> Kg</td>
                                <td style="padding: 10px;"><%= animal.fecha_nacimiento %></td>
                                <td style="padding: 10px; text-align: center; display: flex; gap: 5px; justify-content: center;">
                                    <a href="/editar-ganado/<%= animal.id_animal %>" style="background: #2a9d8f; color: white; padding: 5px 10px; border-radius: 5px; text-decoration: none;">
                                        <i class="fa-solid fa-pen-to-square"></i>
                                    </a>

                                    <form action="/auth/eliminar-animal/<%= animal.id_animal %>" method="POST" onsubmit="return confirm('¿Estás seguro de eliminar este registro?');">
                                        <button type="submit" style="background: #e63946; color: white; border: none; padding: 5px 10px; border-radius: 5px; cursor: pointer;">
                                            <i class="fa-solid fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p style="text-align: center; color: #666; padding: 20px;">
                    No hay animales registrados aún. ¡Usa el botón "Registrar Ganado" para comenzar!
                </p>
            <% } %>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>